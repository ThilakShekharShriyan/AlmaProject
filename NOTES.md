# Attribution

The application code in `apps/web`, the CI workflow, `README.md`, `docs/DESIGN.md`, and `docs/diagrams/` was generated by the Cursor agent and then reviewed in the browser or with `tsc` before push. Commits on `main` were made by the agent when I asked to commit or push.

I did not hand-write those files. I directed the product choices and rejected bad output:

- `apps/web/app/apply/apply-form.tsx`: I hit `Cannot read properties of null (reading 'reset')` after a successful submit. The agent had reset `event.currentTarget` after `await`. The fix keeps the form element from before the request.
- Header, signed-in gate, and brand: I rejected a nav that always showed Admin and Log out, the footer line, and the name Alma. The agent changed those after review.

There is no separately hand-written application module.
